# AI Use Statement

I used an AI assistant (ChatGPT) to help with Lab 4.

Specifically, I used it to:
- Add Maven dependencies for JUnit and JColor in `pom.xml`.
- Write unit tests for `Col.from(...)` and the public interface of `Board`.
- Identify and fix a bug in `Board`'s diagonal winner logic.
- Add JColor-based colouring to the console output in `lab4.ui.Console`.

I reviewed the changes, ran the tests locally (`mvn test`), and ensured I understand the code that I am submitting.
